#include "holberton.h"
/**
 * _isupper - checks for uppercase character.
 *
 * Returns 1 if c is uppercase
 * Returns 0 otherwise
 */
int _isupper(int c)
{
	int i;

	for(i = 65; i <= 90; i++)
	{
		if(check == c)
		{
			return (1);
		}
	}
	return (0);
}
