#include "holberton.h"
/**
 * mul - function that multiplies two integers.
 * a: is the input
 * b: is the input
 * Return: 0
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
