#include "holberton.h"
/**
 * print_chessboard - function that prints the chessboard.
 * @a: input
 * Return: output
 */
void print_chessboard(char (*a)[8])
{
	int i;
	int j;
	char x;

	for (i = 0; a[i] < 8; i++)
	{
		for (j = 0; a[j] < 8; j++)
		{
			x = a[i];
			return (x);
		}
	}
}
